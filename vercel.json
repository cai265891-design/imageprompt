{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "SKIP_ENV_VALIDATION=true bun install && cd apps/nextjs && SKIP_ENV_VALIDATION=true bunx contentlayer2 build && SKIP_ENV_VALIDATION=true bunx next build",
  "installCommand": "echo 'Skip install - handled by buildCommand'",
  "outputDirectory": "apps/nextjs/.next",
  "framework": null,
  "env": {
    "SKIP_ENV_VALIDATION": "true"
  },
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "X-Content-Type-Options",
          "value": "nosniff"
        },
        {
          "key": "X-Frame-Options",
          "value": "DENY"
        },
        {
          "key": "X-XSS-Protection",
          "value": "1; mode=block"
        }
      ]
    }
  ]
}
