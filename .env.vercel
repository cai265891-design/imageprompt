# Clerk Configuration for Vercel
# Use these values in Vercel environment variables

# Dynamic URLs - will work with any language
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/image-prompt
NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL=/image-prompt

# Note: The actual language prefix will be handled by the application routing
# Do NOT include language prefix in these URLs
