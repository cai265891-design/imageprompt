{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "cd ../.. && bun install",
  "buildCommand": "SKIP_ENV_VALIDATION=true bunx contentlayer2 build && SKIP_ENV_VALIDATION=true bunx next build",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "env": {
    "SKIP_ENV_VALIDATION": "true"
  }
}